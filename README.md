# Pipedrive API v1 using example.
-------------

### Start production mode.

```bash
npm install --production;
npm start;
```
### Start dev mode.
Watch and compile changed files.
```bash
npm install;
npm watch;
npm start;
```